{{- if and .Values.istio .Values.istio.enabled }}
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: {{ include "core-user-service.fullname" . }}
  labels:
    {{- include "core-user-service.labels" . | nindent 4 }}
spec:
  hosts:
    - {{ include "core-user-service.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            host: {{ include "core-user-service.fullname" . }}
            port:
              number: {{ .Values.service.port }}
{{- end }}