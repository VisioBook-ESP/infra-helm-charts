apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: application-visiobook
  namespace: argocd
spec:
  destination:
    namespace: visiobook-namespace
    server: https://kubernetes.default.svc

  sources:

    # CloudNative-PG Operator (sync-wave 1)
    - repoURL: https://cloudnative-pg.github.io/charts
      chart: cloudnative-pg
      targetRevision: 0.22.1
      helm:
        values: |
          image:
            tag: "1.24.0"
          monitoring:
            podMonitorEnabled: false

    # PostgreSQL Cluster (sync-wave 2)
    - repoURL: https://github.com/VisioBook-ESP/infra-helm-charts  # Your repo
      path: environnement/dev/charts/cloud-native-postgres/  # or postgres-cluster/charts/
      targetRevision: 35-creer-un-chart-cnpg
      helm:
        valueFiles:
          - values.yaml
        values: |
          existingSecret: "central-secret"

    # Database secrets (managed in infra repo)
    - repoURL: https://github.com/VisioBook-ESP/infra-helm-charts
      path: environnement/dev/app/configs/secrets/
      targetRevision: 35-creer-un-chart-cnpg
      directory:
        recurse: false
    # - repoURL: https://github.com/VisioBook-ESP/support-storage-service
    #   path: charts/
    #   targetRevision: dev
    #   helm:
    #     valueFiles:
    #       - values.yaml

    # - repoURL: https://github.com/VisioBook-ESP/ai-analysis-service
    #   path: charts/
    #   targetRevision: dev
    #   helm:
    #     valueFiles:
    #       - values.yaml



    # Core User Service
    - repoURL: https://github.com/VisioBook-ESP/core-user-service
      path: charts/
      targetRevision: dev
      helm:
        valueFiles:
          - values.yaml
        values: |
          existingSecret: "central-secret"
          env:
            ENV: prod
            LOG_LEVEL: warn

    - repoURL: https://github.com/VisioBook-ESP/infra-helm-charts
      path: environnement/dev/charts/content-ingestion-service/
      targetRevision: 35-creer-un-chart-cnpg
      helm:
        valueFiles:
          - values.yaml
        values: |
          existingSecret: "central-secret"
          env:
            ENV: prod
            LOG_LEVEL: warn

  project: application-visobook

  # Ignore operator-managed fields
  ignoreDifferences:
    # Ignore common Kubernetes auto-managed fields globally
    - kind: "*"
      jsonPointers:
        - /metadata/resourceVersion
        - /metadata/generation

    # Ignore CNPG operator-managed fields on Cluster
    - group: postgresql.cnpg.io
      kind: Cluster
      jsonPointers:
        - /status
        - /spec/imageName

    # Ignore CNPG webhook CA bundles (injected by operator)
    - kind: MutatingWebhookConfiguration
      jsonPointers:
        - /webhooks/0/clientConfig/caBundle
        - /webhooks/1/clientConfig/caBundle
    - kind: ValidatingWebhookConfiguration
      jsonPointers:
        - /webhooks/0/clientConfig/caBundle
        - /webhooks/1/clientConfig/caBundle

    # Ignore CRD conversion webhook CA and fields managed by the API server
    - kind: CustomResourceDefinition
      jsonPointers:
        - /spec/conversion/webhook/clientConfig/caBundle
        - /status

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - RespectIgnoreDifferences=true
      - SkipDryRunOnMissingResource=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m