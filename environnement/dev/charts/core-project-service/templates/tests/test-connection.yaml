apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "core-project-service.fullname" . }}-test-connection"
  labels:
    {{- include "core-project-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "core-project-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
