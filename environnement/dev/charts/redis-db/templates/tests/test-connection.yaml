apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "redis-db.fullname" . }}-test-connection"
  labels:
    {{- include "redis-db.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "redis-db.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
