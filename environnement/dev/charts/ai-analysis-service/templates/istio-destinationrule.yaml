{{- if and .Values.istio.enabled .Values.istio.destinationRule.enabled }}
apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: {{ include "ai-analysis-service.fullname" . }}
  labels:
    {{- include "ai-analysis-service.labels" . | nindent 4 }}
spec:
  host: {{ include "ai-analysis-service.fullname" . }}
  trafficPolicy:
    {{- toYaml .Values.istio.destinationRule.trafficPolicy | nindent 4 }}
    {{- if .Values.istio.mtls.enabled }}
    tls:
      mode: {{ .Values.istio.mtls.mode }}
    {{- end }}
{{- end }}