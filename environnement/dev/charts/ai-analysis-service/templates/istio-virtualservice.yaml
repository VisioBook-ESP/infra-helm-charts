{{- if and .Values.istio .Values.istio.enabled }}
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: {{ include "ai-analysis-service.fullname" . }}
  labels:
    {{- include "ai-analysis-service.labels" . | nindent 4 }}
spec:
  hosts:
    - {{ include "ai-analysis-service.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            host: {{ include "ai-analysis-service.fullname" . }}
            port:
              number: {{ .Values.service.port }}
{{- end }}