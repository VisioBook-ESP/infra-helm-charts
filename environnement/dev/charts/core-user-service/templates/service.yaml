apiVersion: v1
kind: Service
metadata:
  name: {{ include "core-user-service.fullname" . }}
  labels:
    {{- include "core-user-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "core-user-service.selectorLabels" . | nindent 4 }}
