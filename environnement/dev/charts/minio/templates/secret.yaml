apiVersion: v1
kind: Secret
metadata:
  name: {{ include "minio.fullname" . }}-secret
  labels:
    {{- include "minio.labels" . | nindent 4 }}
type: Opaque
data:
  root-user: {{ .Values.minio.auth.rootUser | b64enc }}
  root-password: {{ .Values.minio.auth.rootPassword | b64enc }}
