apiVersion: v1
kind: Service
metadata:
  name: {{ include "service-db.fullname" . }}
  labels:
    {{- include "service-db.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "service-db.selectorLabels" . | nindent 4 }}
