apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "content-injection-service.fullname" . }}-test-connection"
  labels:
    {{- include "content-injection-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "content-injection-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
