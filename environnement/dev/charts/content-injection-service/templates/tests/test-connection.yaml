apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "content-ingestion-service.fullname" . }}-test-connection"
  labels:
    {{- include "content-ingestion-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "content-ingestion-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
