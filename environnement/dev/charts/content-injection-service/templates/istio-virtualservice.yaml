{{- if and .Values.istio .Values.istio.enabled }}
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: {{ include "content-injection-services.fullname" . }}
  labels:
    {{- include "content-injection-services.labels" . | nindent 4 }}
spec:
  hosts:
    - {{ include "content-injection-services.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            host: {{ include "content-injection-services.fullname" . }}
            port:
              number: {{ .Values.service.port }}
{{- end }}