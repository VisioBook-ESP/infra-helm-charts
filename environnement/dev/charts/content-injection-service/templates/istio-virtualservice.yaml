{{- if and .Values.istio .Values.istio.enabled }}
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: {{ include "content-injection-service.fullname" . }}
  labels:
    {{- include "content-injection-service.labels" . | nindent 4 }}
spec:
  hosts:
    - {{ include "content-injection-service.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            host: {{ include "content-injection-service.fullname" . }}
            port:
              number: {{ .Values.service.port }}
{{- end }}