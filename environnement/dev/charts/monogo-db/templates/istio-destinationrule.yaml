{{- if .Values.istio.enabled }}
apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: {{ include "monogo-db.fullname" . }}
  labels:
    {{- include "monogo-db.labels" . | nindent 4 }}
spec:
  host: {{ include "monogo-db.fullname" . }}
  trafficPolicy:
    connectionPool:
      tcp:
        maxConnections: {{ .Values.istio.connectionPool.maxConnections | default 100 }}
        connectTimeout: 30s
    loadBalancer:
      simple: ROUND_ROBIN
    {{- if .Values.istio.mtls.enabled }}
    tls:
      mode: {{ .Values.istio.mtls.mode | default "ISTIO_MUTUAL" }}
    {{- end }}
{{- end }}